// TODO(Phase 2): SVG/div 오버레이 기반 박스 드래그 구현
// - 마우스 drag로 rect 그리기
// - 라벨 종류: borehole_id, coordinate, elevation, groundwater,
//   strata_table_depth, strata_table_soil, project_name
// - 좌표는 페이지 기준 0~1 정규화값으로 저장
export default function BoxOverlay() {
  return <div className="box-overlay-placeholder" />
}
